modelguard-watch is deployed.

Follow logs:
  kubectl logs -f -l "app.kubernetes.io/instance={{ .Release.Name }}" -n {{ .Release.Namespace }}

{{- if not .Values.existingSecret }}

No existingSecret was set, so this release created its own Secret from
values passed to `helm install`/`helm upgrade`. That is the quick-demo path:
those values are stored in this release's history in plain text
(`helm get values {{ .Release.Name }}` shows them). For anything beyond a
local Quickstart, create the Secret yourself and set existingSecret instead.
{{- end }}

{{- if and (not .Values.watch.noLlm) (not .Values.watch.llmProvider) }}

No watch.llmProvider set and --no-llm was not passed: `modelguard watch` will
run with the deterministic template narrator, which is a correct and
supported mode, not a misconfiguration. Set watch.llmProvider/llmModel and
the llmApiKey secret key if you want narrated prose instead.
{{- end }}
