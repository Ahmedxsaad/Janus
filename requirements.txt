# ModelGuard dependencies. Source: docs/plan/02-implementation-plan.md section 1.
#
# Rule: pin exact versions (pkg==x.y.z) at first real install, then keep them
# pinned. Before using any SDK symbol from the plan, verify it against the
# installed package (pip show <pkg>, then introspect): imports drift between
# versions and the plan flags unverified symbols with [confirm].

acryl-datahub[datahub-rest]        # DataHub SDK + REST emitter
datahub-agent-context              # DataHub Agent Context Kit toolset
langgraph                          # orchestration (human-gated state machine)
langchain                          # agent framework
langchain-anthropic                # Claude LLM binding
pydantic                           # typed finding models
pyyaml                             # assertion / contract YAML artifacts
rich                               # CLI output
typer                              # CLI entry points (scan / watch)

# Development and testing
pytest
