# The model side: MLflow's registered models, versions, and runs.
source:
  type: mlflow
  config:
    tracking_uri: "${MLFLOW_TRACKING_URI}"
sink:
  type: datahub-rest
  config:
    server: "${DATAHUB_GMS_URL}"
