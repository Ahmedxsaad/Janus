apiVersion: v3.1.0
kind: DataContract
id: janus.input-contract.credit_risk_v3
name: Input data contract for Credit Risk v3
version: 1.0.0
status: active
description:
  purpose: 'Input requirements Janus derived for the model Credit Risk v3: the schema of every table
    it trains on, and the freshness those tables must hold. Derived from column-level lineage and schema
    metadata in DataHub.'
  usage: Generated by Janus for urn:li:mlModel:(urn:li:dataPlatform:mlflow,credit_risk_v3,PROD).
schema:
- name: ecommerce.public.customer_features
  physicalName: ecommerce.public.customer_features
  logicalType: object
  description: Input table consumed by the model. Freshness is expected on `updated_at`.
  properties:
  - name: applicant_id
    physicalType: VARCHAR
    required: true
    logicalType: string
  - name: applicant_income
    physicalType: NUMBER
    required: true
    logicalType: number
  - name: prior_default_flag
    physicalType: BOOLEAN
    required: true
    logicalType: boolean
  - name: updated_at
    physicalType: TIMESTAMP
    required: true
    logicalType: timestamp
slaProperties:
- property: frequency
  value: 6.0
  unit: h
  element: ecommerce.public.customer_features.updated_at
  driver: operational
  description: Janus freshness SLA; matches the emitted guarding assertion.
